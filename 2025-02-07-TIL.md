# GIT TIL 49 - 장고 미니 프로젝트 실습 3일 차

<br>

## CRUD 구현

<br>

###  구현하는 순서 공식
  - model → form → view → url → template

> 모델은 이미 제작했으니 폼과 뷰를 작성해보자.

## 1. form 작성


```python
from django import forms
from .models import SpartaFurniture

class SpartaFurniture_Form(forms.ModelForm):
    class Meta: 
        model = SpartaFurniture
        fields = [ "sparta_furniture_id",
                   "sparta_furniture_location",
                   "sparta_furniture_manage",
                   "sparta_furniture_tel",
                   "sparta_furniture_open",
                   "sparta_staff_num",
                ]

```


## 2. view 작성


```python
from django.shortcuts import render,redirect

from sparta_furniture.models import SpartaFurniture
from sparta_furniture.foms import SpartaFurniture_Form


# 조회
def market_info(request):
    if request.method == "GET":
        form = SpartaFurniture_Form.objercts.all()
        context = { "form": form }
        return render(request,"market_info",context)


# 생성
def market_create(request):
    if request.method == "POST":
        form = SpartaFurniture_Form(request.POST)
        if form.is_valid():
           sparta_furniture = form.save()
           return redirect("market_info",sparta_furniture)
    else:
        form = SpartaFurniture_Form()

    context = { "form": form }
    return render(request,"market_info",context)


# 삭제          
def market_delete(request, pk):
    if request.method == "POST":
        return redirect("market_info")

# 수정
def market_update(request):
    if request.method == "POST":
        form = SpartaFurniture_Form(request.POST, instance=form)
        pass
#까지만 구현
```



<br>

아직 제작중.. 제작 완료 되는대로 업데이트 할 예정
